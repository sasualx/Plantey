import {Button, View} from 'react-native';
import * as React from 'react';
import {Colors} from '../Colors';

export const HomeScreen = ({navigation}) => {
  return <View style={{backgroundColor: Colors.base}} />;
};
